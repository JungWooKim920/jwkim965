server:
  servlet:
    encoding:
      force-response: true
 
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/test
    username: root
    password: 1111
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
